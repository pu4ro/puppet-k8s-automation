apiVersion: v1
kind: Pod
metadata:
  name: kube-vip
  namespace: kube-system
spec:
  containers:
  - name: kube-vip
    image: <%= @kube_vip_image %>
    env:
    - name: vip_address
      value: "<%= @kube_vip_address %>"
    - name: vip_port
      value: "<%= @kube_vip_port %>"
